# Orientações do projeto

- Manter assinaturas, protótipos, chamadas de função e expressões com múltiplas condições em uma única linha, sem quebrar parâmetros ou condições por limite de largura.

- A cada alteração nas responsabilidades do aluno 2, atualizar `requisitos_aluno_2.md` com o estado real, evidências de teste e pendências. Não marcar previsão como implementação concluída.
- Não modificar `requisitos_aluno_1.md` sem uma nova instrução explícita do usuário.
- Manter comentários explicativos em português nos arquivos C do aluno 2 (`node.c`, `superpeer.c` e seus testes) e atualizar `explicacao_codigo_aluno_2.md` quando o comportamento explicado mudar.
- Distinguir a API local da integração via rede e testes executados de verificações ainda pendentes.
